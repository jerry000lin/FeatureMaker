services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: featuremaker
      POSTGRES_PASSWORD: featuremaker
      POSTGRES_DB: featuremaker
    ports:
      - "65432:5432"
    volumes:
      - featuremaker-postgres:/var/lib/postgresql/data

volumes:
  featuremaker-postgres: